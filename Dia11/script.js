const btn = document.getElementById("btn");

// Adiciona um evento de clique
btn.addEventListener("click", function() {
    // Alterna a classe 'mudado' no botão
    btn.classList.toggle("changed");
});
